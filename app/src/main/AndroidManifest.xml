<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.receiverdemo">

    <!-- Permission pour recevoir BOOT_COMPLETED -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RECEIVER STATIQUE pour BOOT_COMPLETED -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- CustomEventReceiver déclaré en STATIQUE (optionnel, mais nous l'utiliserons dynamiquement dans l'activité) -->
        <!-- Pour l'exemple, on ne le déclare pas dans le manifeste pour montrer l'enregistrement dynamique -->
        <!-- Si vous voulez le rendre statique, décommentez : -->
        <!--
        <receiver
            android:name=".CustomEventReceiver"
            android:exported="false" />
        -->

    </application>

</manifest>